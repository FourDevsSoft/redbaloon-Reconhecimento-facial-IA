# Exemplo de arquivo .env para o Identity Service

# Chave de segurança para comunicação (Obrigatório para rotas não /health)
# No EasyPanel de produção, troque para um valor forte, como um UUID ou hash longo.
API_KEY=sua_chave_em_producao_aqui
